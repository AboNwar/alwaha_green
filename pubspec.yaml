name: alwaha_green
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # الإصدارات الصحيحة والمستقرة
  flutter_local_notifications: ^17.1.2
  url_launcher: ^6.2.0
  flutter_map: ^6.1.0   # <-- الإصدار الصحيح هو 6.1.0
  latlong2: ^0.9.1
  sqflite: ^2.3.3+1
  sqflite_common_ffi: ^2.3.3+1
  geolocator: ^12.0.0
  http: ^1.2.1
  timezone: ^0.9.4
  intl: ^0.19.0
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0
  flutter_launcher_icons: ^0.13.1
flutter_icons:
  android: true
  ios: true
  image_path: "assets/icon.png"

flutter:
  uses-material-design: true
  assets:
    - assets/privacy.html

